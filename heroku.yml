setup:
  addons:
    - plan: 'heroku-postgresql:private-0'
      as: DATABASE
    - plan: 'heroku-redis:private-7'
      as: REDIS
build:
  languages:
    - python
run:
  web: 'gunicorn --bind 0.0.0.0:$PORT wsgi'
