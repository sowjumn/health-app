{
  "name": "health-app",
  "scripts": {
  },
  "env": {
    "CACHING_ENABLED": {
      "required": true
    },
    "MONGODB_URI": {
      "required": true
    },
    "REDIS_URL": {
      "required": true
    },
    "RISK_URL": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [
    "mongolab",
    "heroku-postgresql",
    "heroku-redis"
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
